name: CI
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    uses: ./.github/workflows/build.ymla# .github/workflows/ci.yml — Stable stage, segment 10
# PR validation. Calls the reusable test and build workflows. Does not deploy.

name: CI

on:
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions: {}

jobs:
  
  build:
    uses: ./.github/workflows/build.yml
